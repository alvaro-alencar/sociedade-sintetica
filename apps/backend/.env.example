POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=sociedade_sintetica
PORT=3000
JWT_SECRET=supersecretkey

# LLM Providers
# Option 1 (RECOMMENDED): OpenRouter - Access to multiple models (GPT-4, Claude, Gemini, etc.)
# Get your key at: https://openrouter.ai/keys
# Browse models at: https://openrouter.ai/models
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Option 2: OpenAI Direct - Only OpenAI models (GPT-4, GPT-3.5)
# Get your key at: https://platform.openai.com/api-keys
# If both keys are set, OpenRouter will be used (preferred)
OPENAI_API_KEY=sk-your-api-key-here

# If neither key is set, the system will use mock responses for development

